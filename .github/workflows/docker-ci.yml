name: Docker Build, Scout and Push

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies and run tests
        run: |
          pip install pytest flask redis
          cd voting-app/azure-vote
          pytest test_main.py

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/voting-app:latest .

      - name: Docker Scout
        uses: docker/scout-action@v1
        with:
          command: cves
          image: ${{ secrets.DOCKER_USERNAME }}/voting-app:latest
          ignore-unchanged: true
          only-severities: critical,high

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/voting-app:latest
